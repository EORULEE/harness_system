# 검증 스위트 (10 smoke, 전부 green)

deploy_gate(18)·code_claim_evidence(13)·ledger_variant_hardening(14)·adaptive_verification·astgrep·serena_readonly·system_truth_index·active_context_emit·shared_knowledge_layer·stop_guard_notification.

실행: `bash bootstrap.sh` 또는 `for s in tests/smoke_*.sh; do bash "$s"; done`.

> 원 시스템의 일부 smoke(개인 배포-구조 integrity 검사·제외한 통합[KB/공유/멀티머신/발표디자인])는 본 공개 코어에서 제외됨.
